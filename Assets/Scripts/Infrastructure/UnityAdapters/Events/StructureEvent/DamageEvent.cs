using UnityEngine;

/// <summary>
/// DamageEvent is a structure that represents an event where a character receives damage.
/// </summary>

public struct DamageEvent
{
    public GameObject Character;
    public float Amount;
}